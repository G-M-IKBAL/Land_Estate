

import React from 'react'

import Snak from '../components/Snak'

const Snak = (severity,msg) => {
  return (
   <div>
    <Snak severity={this.severity} msg={this.msg} flag="true"/>
   </div>
  );
};

export default Snak;